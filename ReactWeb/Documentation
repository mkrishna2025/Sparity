Babel will convert html syntax code(jsx) into react elements

ReactDOM.render(React element to display, DOM element where we need to inject/replace)


React.createElement(What is element type, element properties, display/welcome text)
React.createElement('div', {id: 'dynamicDiv'}, 'Welcome to Demo 2')